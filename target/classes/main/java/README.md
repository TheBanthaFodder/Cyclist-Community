# Source Layout

## Packages
* `controller` - controller classes for each user-facing flow
* `view` - lightweight view classes for each user-facing flow
* `model/application` - controller-facing model coordinators
* `model/race` - race and registration domain classes
* `model/user` - user hierarchy and strategy/context classes
* `model/observer` - observer abstractions used by category updates
